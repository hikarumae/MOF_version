# --- 直接使用しているライブラリ (バージョン指定を強化) ---
pymupdf>=1.24.10    # Error: 1.22.5 -> Fix: 1.24系へ
flask>=3.0.3        # Flaskも新しくしておきます
pypdf>=5.1.0        # Error: 5.0.0 -> Fix: 5.1系へ

fastapi>=0.115.0
uvicorn[standard]
azure-storage-blob
azure-search-documents
openai>=1.53.1
jiter
typing-extensions>=4.11
python-dotenv

# --- 脆弱性対応: サブ依存関係の強制アップデート ---
# これらは本来書かなくても動きますが、Snykのエラーを消すために明記

werkzeug>=3.0.3     # Error: 2.2.3 (Flaskの裏側) -> Fix: 3.0系へ
anyio==4.4.0        # Error: 3.7.0 -> Fix: 4.4.0 (FastAPIの依存関係で、最新の安定版を指定)
requests>=2.32.0    # Error: 2.31.0 -> Fix: 2.32系へ
azure-core>=1.31.0  # Error: 1.30.1 -> Fix: 1.31系へ

# --- 既に記述があったが、念のため最新を維持するもの ---
h11==0.16.0      # FastAPIの依存関係で、最新の安定版を指定
urllib3>=2.2.3      # Error: 2.0.7 -> File: 2.2.2 (念のため2.2.3へ)
cryptography>=46.0.5
zipp==3.19.1        # 依存関係の一部で、最新の安定版を指定